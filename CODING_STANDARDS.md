# LearnBuddy coding standards

本文件是 code-review **标准轴的唯一规则源**。开始标准轴评审时先完整读取本文件；提示词传递本文件路径和 diff，保持规则只存于本文件。

## 断言

- 任何让断言更容易通过的改动，都要在提交说明中写明变松的理由以及仍然保留的约束。例如，把 `resumed === '2'` 改成“任意合法 Reading position”需要解释为什么具体句子不再是稳定不变量。

## 旧壳兼容

- 对 `web/js/**` 下已有共享文件，将改动限制为新增行并保持既有行不变。旧壳 `/` 仍在线使用，其现有行为由 smoke 断言保护。
- 评审共享文件改动时，核对受影响的旧壳 smoke 断言仍在本次验证范围内。

## 键盘输入

- 全局键盘快捷键把文本输入权留给获得焦点的可编辑控件；事件来自 `input`、`textarea`、`select` 或可编辑元素时，由控件处理它拥有的按键，包括空格。
- 每个会操作文本框的 smoke 用例至少包含一次 `keyboard.type(...)`，并断言输入值能保留空格等真实键入行为。`fill(...)` 适合设置前置值，不能代替这项键入覆盖。

## 壳状态

- `/next/` 的 `face` 决定显示内容；`mode` 表示旧壳状态。实现和评审保持两套状态各自明确，不跨壳复用。

## 坏味道

- Fowler 坏味道清单只引用 [_Refactoring_（第二版）](https://martinfowler.com/books/refactoring.html) 的 code-smell 章节，不在评审提示词或本文件中复制清单。坏味道只作为需要进一步检查的判断项；本文件的明确规则优先。
