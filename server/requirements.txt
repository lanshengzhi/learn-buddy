# Chinese word segmentation for epub parsing (server/textseg.py).
# Install: python3 -m venv .venv && .venv/bin/pip install -r server/requirements.txt
sudachipy==0.6.11
sudachidict_core==20260723
jieba==0.42.1
# Optional, isolated NotebookLM worker runtime (issue #73). The worker
# imports this lazily; LearnBuddy host code never depends on its API.
notebooklm-py==0.8.2
